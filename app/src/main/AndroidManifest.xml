<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.esgi.greenrepack">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GreenRepack">
        <activity android:name=".Green_Coin_DetailActivity2"></activity>
        <activity android:name=".associations.Create_SuccessActivity" />
        <activity android:name=".associations.ProjectAddActivity" />
        <activity android:name=".associations.InCurseActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GreenCoinsActivity" />
        <activity android:name=".ProjectsActivity" />
        <activity android:name=".ProjectActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>